require 'spec_helper'

describe Cells::Handlebars do
  it 'has a version number' do
    expect(Cells::Handlebars::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
